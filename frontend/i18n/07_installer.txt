# The Windows installer card on the Deploy page (admin only). Sentences built by script go through t().
Windows installer || Instalator dla Windows || Installateur Windows || Windows-Installer || Instalador de Windows
@connect.installer_help || Tworzy plik instalacyjny (<code>.exe</code>), który instaluje RustDesk i stosuje powyższe serwery, dzięki czemu na komputerze nie trzeba niczego wpisywać. Pobiera pakiet MSI programu RustDesk z GitHuba i pakuje go za pomocą NSIS. <strong>Najpierw wypróbuj na jednym komputerze:</strong> przed instalacją usuwa zainstalowany RustDesk. || Crée un fichier d'installation (<code>.exe</code>) qui installe RustDesk et applique les serveurs ci-dessus, sans rien avoir à saisir sur l'ordinateur. Il télécharge le MSI de RustDesk depuis GitHub et l'emballe avec NSIS. <strong>Essayez d'abord sur une seule machine :</strong> il supprime un RustDesk déjà installé avant d'installer. || Erstellt eine Setup-Datei (<code>.exe</code>), die RustDesk installiert und die obigen Server übernimmt, sodass am Computer nichts eingegeben werden muss. Sie lädt das MSI von RustDesk bei GitHub herunter und verpackt es mit NSIS. <strong>Probieren Sie es zuerst auf einem Rechner aus:</strong> Ein bereits installiertes RustDesk wird vor der Installation entfernt. || Crea un archivo de instalación (<code>.exe</code>) que instala RustDesk y aplica los servidores de arriba, de modo que no haya que escribir nada en el equipo. Descarga el MSI de RustDesk desde GitHub y lo empaqueta con NSIS. <strong>Pruébelo primero en un solo equipo:</strong> elimina un RustDesk ya instalado antes de instalar.
x64 (Intel/AMD, 64-bit) || x64 (Intel/AMD, 64-bitowy) || x64 (Intel/AMD, 64 bits) || x64 (Intel/AMD, 64 Bit) || x64 (Intel/AMD, 64 bits)
Also remove RustDesk’s existing settings first (the computer gets a new ID) || Najpierw usuń też istniejące ustawienia RustDesk (komputer dostanie nowy ID) || Supprimer aussi d'abord les réglages existants de RustDesk (l'ordinateur reçoit un nouvel ID) || Vorher auch die vorhandenen RustDesk-Einstellungen entfernen (der Computer erhält eine neue ID) || Eliminar también antes la configuración existente de RustDesk (el equipo recibe un ID nuevo)
Build installer || Zbuduj instalator || Créer l'installateur || Installer erstellen || Crear instalador
Download build kit (.zip) || Pobierz zestaw do budowania (.zip) || Télécharger le kit de compilation (.zip) || Build-Kit herunterladen (.zip) || Descargar el kit de compilación (.zip)
@connect.kit_help || <strong>Zestaw do budowania</strong> tworzy ten sam plik na dowolnym komputerze z Windows, na którym jest NSIS, i podpisuje go tam, jeśli wskażesz certyfikat (<code>build.ps1 -Thumbprint &hellip;</code>), więc certyfikat nie musi być na tym serwerze. Serwer także może podpisywać: zobacz <code>INSTALLER_SIGN_COMMAND</code> w README. || Le <strong>kit de compilation</strong> crée le même fichier sur n'importe quel ordinateur Windows où NSIS est installé, et le signe là-bas si vous lui indiquez un certificat (<code>build.ps1 -Thumbprint &hellip;</code>), donc le certificat n'a pas besoin d'être sur ce serveur. Le serveur peut aussi signer : voir <code>INSTALLER_SIGN_COMMAND</code> dans le README. || Das <strong>Build-Kit</strong> erstellt dieselbe Datei auf jedem Windows-Computer mit NSIS und signiert sie dort, wenn Sie ein Zertifikat angeben (<code>build.ps1 -Thumbprint &hellip;</code>), sodass das Zertifikat nicht auf diesem Server liegen muss. Auch der Server kann signieren: siehe <code>INSTALLER_SIGN_COMMAND</code> in der README. || El <strong>kit de compilación</strong> crea el mismo archivo en cualquier equipo con Windows que tenga NSIS y lo firma allí si le da un certificado (<code>build.ps1 -Thumbprint &hellip;</code>), de modo que el certificado no tiene que estar en este servidor. El servidor también puede firmar: vea <code>INSTALLER_SIGN_COMMAND</code> en el README.
Files on the server || Pliki na serwerze || Fichiers sur le serveur || Dateien auf dem Server || Archivos en el servidor
Nothing is stored. || Nic nie jest zapisane. || Rien n'est stocké. || Es ist nichts gespeichert. || No hay nada almacenado.
Modified || Zmieniono || Modifié || Geändert || Modificado
Actions || Akcje || Actions || Aktionen || Acciones
Download || Pobierz || Télécharger || Herunterladen || Descargar
Delete all downloaded MSI files || Usuń wszystkie pobrane pliki MSI || Supprimer tous les fichiers MSI téléchargés || Alle heruntergeladenen MSI-Dateien löschen || Eliminar todos los archivos MSI descargados
Delete all built installers || Usuń wszystkie zbudowane instalatory || Supprimer tous les installateurs créés || Alle erstellten Installer löschen || Eliminar todos los instaladores creados

!Stored in {1}. Both kinds are made again when needed, so deleting them is safe. || Zapisane w {1}. Oba rodzaje plików są tworzone ponownie w razie potrzeby, więc ich usunięcie jest bezpieczne. || Stocké dans {1}. Les deux types sont recréés au besoin, les supprimer est donc sans risque. || Gespeichert in {1}. Beide Arten werden bei Bedarf neu erzeugt, das Löschen ist also unbedenklich. || Almacenados en {1}. Ambos tipos se vuelven a crear cuando hace falta, así que eliminarlos es seguro.
!RustDesk MSI (downloaded) || RustDesk MSI (pobrany) || MSI RustDesk (téléchargé) || RustDesk-MSI (heruntergeladen) || MSI de RustDesk (descargado)
!Installer (built here) || Instalator (zbudowany tutaj) || Installateur (créé ici) || Installer (hier erstellt) || Instalador (creado aquí)
!Delete all downloaded RustDesk MSI files? || Usunąć wszystkie pobrane pliki MSI RustDesk? || Supprimer tous les fichiers MSI RustDesk téléchargés ? || Alle heruntergeladenen RustDesk-MSI-Dateien löschen? || ¿Eliminar todos los archivos MSI de RustDesk descargados?
!Delete all built installers? || Usunąć wszystkie zbudowane instalatory? || Supprimer tous les installateurs créés ? || Alle erstellten Installer löschen? || ¿Eliminar todos los instaladores creados?
!Delete {1}? || Usunąć {1}? || Supprimer {1} ? || {1} löschen? || ¿Eliminar {1}?
!Deleted {1} file(s). || Usunięto plików: {1}. || {1} fichier(s) supprimé(s). || {1} Datei(en) gelöscht. || {1} archivo(s) eliminado(s).

!nightly {1} (pre-release) || nightly {1} (wersja wstępna) || nightly {1} (préversion) || Nightly {1} (Vorabversion) || nightly {1} (versión preliminar)
!{1} (pre-release) || {1} (wersja wstępna) || {1} (préversion) || {1} (Vorabversion) || {1} (versión preliminar)
!{1} (latest stable) || {1} (najnowsza stabilna) || {1} (dernière stable) || {1} (neueste stabile) || {1} (última estable)
!signed || podpisany || signé || signiert || firmado
!not signed || niepodpisany || non signé || nicht signiert || sin firmar
!Download {1} || Pobierz {1} || Télécharger {1} || {1} herunterladen || Descargar {1}
!Downloading RustDesk from GitHub... {1}% || Pobieranie RustDesk z GitHuba... {1}% || Téléchargement de RustDesk depuis GitHub... {1}% || RustDesk wird von GitHub heruntergeladen... {1}% || Descargando RustDesk desde GitHub... {1}%
!Building the installer... || Budowanie instalatora... || Création de l'installateur... || Installer wird erstellt... || Creando el instalador...
!Starting... || Uruchamianie... || Démarrage... || Wird gestartet... || Iniciando...
!Enter the ID server first. || Najpierw podaj serwer ID. || Saisissez d'abord le serveur ID. || Geben Sie zuerst den ID-Server ein. || Introduzca primero el servidor de ID.
!The installer could not be built. || Nie udało się zbudować instalatora. || L'installateur n'a pas pu être créé. || Der Installer konnte nicht erstellt werden. || No se pudo crear el instalador.
!The list of RustDesk releases could not be loaded from GitHub. Try again in a moment. || Nie udało się wczytać listy wydań RustDesk z GitHuba. Spróbuj ponownie za chwilę. || La liste des versions de RustDesk n'a pas pu être chargée depuis GitHub. Réessayez dans un instant. || Die Liste der RustDesk-Versionen konnte nicht von GitHub geladen werden. Versuchen Sie es gleich noch einmal. || No se pudo cargar la lista de versiones de RustDesk desde GitHub. Inténtelo de nuevo en un momento.
!NSIS (makensis) is not installed on this server, so it cannot build the file itself. Use the build kit on a Windows computer that has NSIS. || NSIS (makensis) nie jest zainstalowany na tym serwerze, więc serwer nie zbuduje pliku sam. Użyj zestawu do budowania na komputerze z Windows, na którym jest NSIS. || NSIS (makensis) n'est pas installé sur ce serveur, qui ne peut donc pas créer le fichier lui-même. Utilisez le kit de compilation sur un ordinateur Windows où NSIS est installé. || NSIS (makensis) ist auf diesem Server nicht installiert, daher kann er die Datei nicht selbst erstellen. Verwenden Sie das Build-Kit auf einem Windows-Computer mit NSIS. || NSIS (makensis) no está instalado en este servidor, así que no puede crear el archivo por sí mismo. Use el kit de compilación en un equipo con Windows que tenga NSIS.
!Building on the server is turned off (INSTALLER_BUILD_ENABLED). The build kit still works. || Budowanie na serwerze jest wyłączone (INSTALLER_BUILD_ENABLED). Zestaw do budowania nadal działa. || La création sur le serveur est désactivée (INSTALLER_BUILD_ENABLED). Le kit de compilation fonctionne toujours. || Das Erstellen auf dem Server ist abgeschaltet (INSTALLER_BUILD_ENABLED). Das Build-Kit funktioniert weiterhin. || La creación en el servidor está desactivada (INSTALLER_BUILD_ENABLED). El kit de compilación sigue funcionando.
!Ready to build here. The file is signed by the server (INSTALLER_SIGN_COMMAND). || Gotowe do budowania tutaj. Plik zostanie podpisany przez serwer (INSTALLER_SIGN_COMMAND). || Prêt à créer ici. Le fichier est signé par le serveur (INSTALLER_SIGN_COMMAND). || Bereit zum Erstellen. Die Datei wird vom Server signiert (INSTALLER_SIGN_COMMAND). || Listo para crear aquí. El archivo lo firma el servidor (INSTALLER_SIGN_COMMAND).
!Ready to build here. The file is not signed: sign it yourself, or use the build kit on the computer that has your certificate. || Gotowe do budowania tutaj. Plik nie jest podpisany: podpisz go samodzielnie albo użyj zestawu do budowania na komputerze z Twoim certyfikatem. || Prêt à créer ici. Le fichier n'est pas signé : signez-le vous-même, ou utilisez le kit de compilation sur l'ordinateur qui a votre certificat. || Bereit zum Erstellen. Die Datei ist nicht signiert: Signieren Sie sie selbst oder verwenden Sie das Build-Kit auf dem Computer mit Ihrem Zertifikat. || Listo para crear aquí. El archivo no está firmado: fírmelo usted mismo o use el kit de compilación en el equipo que tiene su certificado.

# ---- code signing certificate
Sign the installer with the uploaded certificate || Podpisz instalator przesłanym certyfikatem || Signer l'installateur avec le certificat téléversé || Installer mit dem hochgeladenen Zertifikat signieren || Firmar el instalador con el certificado subido
Code signing certificate || Certyfikat do podpisywania kodu || Certificat de signature de code || Zertifikat für die Codesignatur || Certificado de firma de código
@connect.cert_help || Podpisany instalator nie wywołuje ostrzeżenia systemu Windows o „nieznanym wydawcy”. Prześlij swój certyfikat do podpisywania kodu (plik <code>.pfx</code> lub <code>.p12</code> wraz z hasłem), aby podpisywać instalatory budowane tutaj. Jest przechowywany w postaci zaszyfrowanej, nie można go ponownie pobrać i korzysta z niego każdy administrator budujący instalator. || Un installateur signé ne déclenche pas l'avertissement « éditeur inconnu » de Windows. Téléversez votre certificat de signature de code (un fichier <code>.pfx</code> ou <code>.p12</code> avec son mot de passe) pour signer les installateurs créés ici. Il est stocké chiffré, ne peut jamais être retéléchargé et sert à chaque administrateur qui crée un installateur. || Ein signierter Installer löst die Windows-Warnung „Unbekannter Herausgeber“ nicht aus. Laden Sie Ihr Zertifikat für die Codesignatur hoch (eine <code>.pfx</code>- oder <code>.p12</code>-Datei mit Passwort), um die hier erstellten Installer zu signieren. Es wird verschlüsselt gespeichert, kann nie wieder heruntergeladen werden und wird von jedem Administrator genutzt, der einen Installer erstellt. || Un instalador firmado no muestra la advertencia de Windows de «editor desconocido». Suba su certificado de firma de código (un archivo <code>.pfx</code> o <code>.p12</code> con su contraseña) para firmar los instaladores creados aquí. Se guarda cifrado, no se puede volver a descargar y lo usa cualquier administrador que cree un instalador.
No certificate is stored, so the installers are not signed. || Nie zapisano żadnego certyfikatu, więc instalatory nie są podpisane. || Aucun certificat n'est stocké, les installateurs ne sont donc pas signés. || Es ist kein Zertifikat gespeichert, die Installer sind also nicht signiert. || No hay ningún certificado guardado, así que los instaladores no están firmados.
Issued to || Wystawiony dla || Délivré à || Ausgestellt für || Emitido para
Issued by || Wystawiony przez || Délivré par || Ausgestellt von || Emitido por
Thumbprint (SHA-1) || Odcisk palca (SHA-1) || Empreinte (SHA-1) || Fingerabdruck (SHA-1) || Huella digital (SHA-1)
Valid until || Ważny do || Valide jusqu'au || Gültig bis || Válido hasta
Uploaded || Przesłano || Téléversé || Hochgeladen || Subido
Certificate file (.pfx or .p12) || Plik certyfikatu (.pfx lub .p12) || Fichier de certificat (.pfx ou .p12) || Zertifikatsdatei (.pfx oder .p12) || Archivo del certificado (.pfx o .p12)
Its password || Jego hasło || Son mot de passe || Sein Passwort || Su contraseña
Upload certificate || Prześlij certyfikat || Téléverser le certificat || Zertifikat hochladen || Subir certificado
Remove certificate || Usuń certyfikat || Supprimer le certificat || Zertifikat entfernen || Quitar certificado
@connect.cert_storage_help || Hasło służy tylko do jednorazowego otwarcia pliku i nie jest zapisywane: serwer przechowuje klucz pod własnym hasłem, zaszyfrowany przy użyciu <code>DATA_ENCRYPTION_KEY</code>. Jeśli wolisz nie mieć klucza na tym serwerze, użyj zestawu do budowania na komputerze, który go ma. || Le mot de passe ne sert qu'à ouvrir le fichier une fois et n'est pas conservé : le serveur stocke la clé sous son propre mot de passe, chiffré avec <code>DATA_ENCRYPTION_KEY</code>. Si vous préférez ne pas avoir la clé sur ce serveur, utilisez le kit de compilation sur l'ordinateur qui l'a. || Das Passwort dient nur dazu, die Datei einmal zu öffnen, und wird nicht gespeichert: Der Server legt den Schlüssel unter einem eigenen Passwort ab, verschlüsselt mit <code>DATA_ENCRYPTION_KEY</code>. Wenn Sie den Schlüssel lieber nicht auf diesem Server haben möchten, verwenden Sie das Build-Kit auf dem Computer, der ihn hat. || La contraseña solo sirve para abrir el archivo una vez y no se guarda: el servidor guarda la clave con una contraseña propia, cifrada con <code>DATA_ENCRYPTION_KEY</code>. Si prefiere no tener la clave en este servidor, use el kit de compilación en el equipo que la tiene.
!Choose a certificate file (.pfx or .p12) first. || Najpierw wybierz plik certyfikatu (.pfx lub .p12). || Choisissez d'abord un fichier de certificat (.pfx ou .p12). || Wählen Sie zuerst eine Zertifikatsdatei (.pfx oder .p12). || Elija primero un archivo de certificado (.pfx o .p12).
!Certificate stored. || Certyfikat zapisany. || Certificat enregistré. || Zertifikat gespeichert. || Certificado guardado.
!Certificate removed. || Certyfikat usunięty. || Certificat supprimé. || Zertifikat entfernt. || Certificado eliminado.
!Remove the certificate and its private key from the server? || Usunąć certyfikat i jego klucz prywatny z serwera? || Supprimer le certificat et sa clé privée du serveur ? || Zertifikat und privaten Schlüssel vom Server entfernen? || ¿Quitar el certificado y su clave privada del servidor?
!Expired || Wygasł || Expiré || Abgelaufen || Caducado
!{1} (expired) || {1} (wygasł) || {1} (expiré) || {1} (abgelaufen) || {1} (caducado)
!{1} by {2} || {1}, przez {2} || {1} par {2} || {1} von {2} || {1} por {2}
!Set DATA_ENCRYPTION_KEY on the server to store a certificate: its private key is kept encrypted with it. || Ustaw DATA_ENCRYPTION_KEY na serwerze, aby przechowywać certyfikat: jego klucz prywatny jest przechowywany zaszyfrowany za jego pomocą. || Définissez DATA_ENCRYPTION_KEY sur le serveur pour stocker un certificat : sa clé privée est conservée chiffrée avec elle. || Setzen Sie DATA_ENCRYPTION_KEY auf dem Server, um ein Zertifikat zu speichern: Sein privater Schlüssel wird damit verschlüsselt aufbewahrt. || Defina DATA_ENCRYPTION_KEY en el servidor para guardar un certificado: su clave privada se guarda cifrada con ella.
!osslsigncode is not installed on this server, so a certificate cannot be used to sign yet. || osslsigncode nie jest zainstalowany na tym serwerze, więc certyfikatu nie można jeszcze użyć do podpisywania. || osslsigncode n'est pas installé sur ce serveur, un certificat ne peut donc pas encore servir à signer. || osslsigncode ist auf diesem Server nicht installiert, daher kann ein Zertifikat noch nicht zum Signieren verwendet werden. || osslsigncode no está instalado en este servidor, así que todavía no se puede firmar con un certificado.
!This certificate has expired: installers are not signed until you upload a new one. || Ten certyfikat wygasł: instalatory nie będą podpisywane, dopóki nie prześlesz nowego. || Ce certificat a expiré : les installateurs ne sont pas signés tant que vous n'en téléversez pas un nouveau. || Dieses Zertifikat ist abgelaufen: Installer werden erst wieder signiert, wenn Sie ein neues hochladen. || Este certificado ha caducado: los instaladores no se firman hasta que suba uno nuevo.
!INSTALLER_SIGN_COMMAND is set, so the server signs with that command and does not use this certificate. || INSTALLER_SIGN_COMMAND jest ustawione, więc serwer podpisuje tym poleceniem i nie używa tego certyfikatu. || INSTALLER_SIGN_COMMAND est défini : le serveur signe avec cette commande et n'utilise pas ce certificat. || INSTALLER_SIGN_COMMAND ist gesetzt: Der Server signiert mit diesem Befehl und verwendet dieses Zertifikat nicht. || INSTALLER_SIGN_COMMAND está definido: el servidor firma con ese comando y no usa este certificado.
!Signatures are timestamped by {1}, so they stay valid after the certificate expires (INSTALLER_TIMESTAMP_URL). || Podpisy mają znacznik czasu z {1}, więc pozostają ważne po wygaśnięciu certyfikatu (INSTALLER_TIMESTAMP_URL). || Les signatures sont horodatées par {1} et restent donc valides après l'expiration du certificat (INSTALLER_TIMESTAMP_URL). || Signaturen erhalten einen Zeitstempel von {1} und bleiben so nach Ablauf des Zertifikats gültig (INSTALLER_TIMESTAMP_URL). || Las firmas llevan sello de tiempo de {1}, así que siguen siendo válidas cuando el certificado caduca (INSTALLER_TIMESTAMP_URL).
!Signatures are not timestamped (INSTALLER_TIMESTAMP_URL is empty): they stop being valid when the certificate expires. || Podpisy nie mają znacznika czasu (INSTALLER_TIMESTAMP_URL jest puste): przestają być ważne, gdy certyfikat wygaśnie. || Les signatures ne sont pas horodatées (INSTALLER_TIMESTAMP_URL est vide) : elles cessent d'être valides à l'expiration du certificat. || Signaturen erhalten keinen Zeitstempel (INSTALLER_TIMESTAMP_URL ist leer): Sie verlieren ihre Gültigkeit, wenn das Zertifikat abläuft. || Las firmas no llevan sello de tiempo (INSTALLER_TIMESTAMP_URL está vacío): dejan de ser válidas cuando el certificado caduca.
!Signing the installer... || Podpisywanie instalatora... || Signature de l'installateur... || Installer wird signiert... || Firmando el instalador...
!Ready to build here. The file is signed with the uploaded certificate unless you untick the box above. || Gotowe do budowania tutaj. Plik zostanie podpisany przesłanym certyfikatem, chyba że odznaczysz pole powyżej. || Prêt à créer ici. Le fichier est signé avec le certificat téléversé, sauf si vous décochez la case ci-dessus. || Bereit zum Erstellen. Die Datei wird mit dem hochgeladenen Zertifikat signiert, sofern Sie das Kästchen oben nicht abwählen. || Listo para crear aquí. El archivo se firma con el certificado subido, salvo que desmarque la casilla de arriba.
The uploaded certificate has expired, so the installer is not signed. || Przesłany certyfikat wygasł, więc instalator nie jest podpisany. || Le certificat téléversé a expiré, l'installateur n'est donc pas signé. || Das hochgeladene Zertifikat ist abgelaufen, daher ist der Installer nicht signiert. || El certificado subido ha caducado, así que el instalador no está firmado.
osslsigncode is not installed on the server, so the installer is not signed. || osslsigncode nie jest zainstalowany na serwerze, więc instalator nie jest podpisany. || osslsigncode n'est pas installé sur le serveur, l'installateur n'est donc pas signé. || osslsigncode ist auf dem Server nicht installiert, daher ist der Installer nicht signiert. || osslsigncode no está instalado en el servidor, así que el instalador no está firmado.
DATA_ENCRYPTION_KEY is not set, so the uploaded certificate cannot be used and the installer is not signed. || DATA_ENCRYPTION_KEY nie jest ustawiony, więc przesłanego certyfikatu nie można użyć, a instalator nie jest podpisany. || DATA_ENCRYPTION_KEY n'est pas défini : le certificat téléversé ne peut pas servir et l'installateur n'est pas signé. || DATA_ENCRYPTION_KEY ist nicht gesetzt, daher kann das hochgeladene Zertifikat nicht verwendet werden und der Installer ist nicht signiert. || DATA_ENCRYPTION_KEY no está definido, así que no se puede usar el certificado subido y el instalador no está firmado.
