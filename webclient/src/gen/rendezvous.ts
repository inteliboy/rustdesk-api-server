// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: rendezvous.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hbb";

export enum ConnType {
  DEFAULT_CONN = 0,
  FILE_TRANSFER = 1,
  PORT_FORWARD = 2,
  RDP = 3,
  VIEW_CAMERA = 4,
  TERMINAL = 5,
}

export enum NatType {
  UNKNOWN_NAT = 0,
  ASYMMETRIC = 1,
  SYMMETRIC = 2,
}

export interface RegisterPeer {
  id: string;
  serial: number;
}

export interface RegisterPeerResponse {
  request_pk: boolean;
}

export interface PunchHoleRequest {
  id: string;
  nat_type: NatType;
  licence_key: string;
  conn_type: ConnType;
  token: string;
  version: string;
  udp_port: number;
  force_relay: boolean;
  upnp_port: number;
  socket_addr_v6: Uint8Array;
  switch_code: string;
}

export interface ControlPermissions {
  permissions: bigint;
}

export enum ControlPermissions_Permission {
  keyboard = 0,
  remote_printer = 1,
  clipboard = 2,
  file = 3,
  audio = 4,
  camera = 5,
  terminal = 6,
  tunnel = 7,
  restart = 8,
  recording = 9,
  block_input = 10,
  remote_modify = 11,
  privacy_mode = 12,
}

export interface ControlledContext {
  conn_audit_ref: string;
}

export interface PunchHole {
  socket_addr: Uint8Array;
  relay_server: string;
  nat_type: NatType;
  udp_port: number;
  force_relay: boolean;
  upnp_port: number;
  socket_addr_v6: Uint8Array;
  control_permissions?: ControlPermissions | undefined;
  controlled_context?: ControlledContext | undefined;
}

export interface TestNatRequest {
  serial: number;
}

/** per my test, uint/int has no difference in encoding, int not good for negative, use sint for negative */
export interface TestNatResponse {
  port: number;
  /** for mobile */
  cu?: ConfigUpdate | undefined;
}

export interface PunchHoleSent {
  socket_addr: Uint8Array;
  id: string;
  relay_server: string;
  nat_type: NatType;
  version: string;
  upnp_port: number;
  socket_addr_v6: Uint8Array;
}

export interface RegisterPk {
  id: string;
  uuid: Uint8Array;
  pk: Uint8Array;
  old_id: string;
  no_register_device: boolean;
}

export interface RegisterPkResponse {
  result: RegisterPkResponse_Result;
  keep_alive: number;
}

export enum RegisterPkResponse_Result {
  OK = 0,
  UUID_MISMATCH = 2,
  ID_EXISTS = 3,
  TOO_FREQUENT = 4,
  INVALID_ID_FORMAT = 5,
  NOT_SUPPORT = 6,
  SERVER_ERROR = 7,
  NOT_DEPLOYED = 8,
}

export interface PunchHoleResponse {
  socket_addr: Uint8Array;
  pk: Uint8Array;
  failure: PunchHoleResponse_Failure;
  relay_server: string;
  union?: { $case: "nat_type"; nat_type: NatType } | { $case: "is_local"; is_local: boolean } | undefined;
  other_failure: string;
  feedback: number;
  is_udp: boolean;
  upnp_port: number;
  socket_addr_v6: Uint8Array;
}

export enum PunchHoleResponse_Failure {
  ID_NOT_EXIST = 0,
  OFFLINE = 2,
  LICENSE_MISMATCH = 3,
  LICENSE_OVERUSE = 4,
}

export interface ConfigUpdate {
  serial: number;
  rendezvous_servers: string[];
}

export interface RequestRelay {
  id: string;
  uuid: string;
  socket_addr: Uint8Array;
  relay_server: string;
  secure: boolean;
  licence_key: string;
  conn_type: ConnType;
  token: string;
  control_permissions?: ControlPermissions | undefined;
  controlled_context?: ControlledContext | undefined;
  switch_code: string;
}

export interface RelayResponse {
  socket_addr: Uint8Array;
  uuid: string;
  relay_server: string;
  union?: { $case: "id"; id: string } | { $case: "pk"; pk: Uint8Array } | undefined;
  refuse_reason: string;
  version: string;
  feedback: number;
  socket_addr_v6: Uint8Array;
  upnp_port: number;
}

export interface SoftwareUpdate {
  url: string;
}

/**
 * if in same intranet, punch hole won't work both for udp and tcp,
 * even some router has below connection error if we connect itself,
 *  { kind: Other, error: "could not resolve to any address" },
 * so we request local address to connect.
 */
export interface FetchLocalAddr {
  socket_addr: Uint8Array;
  relay_server: string;
  socket_addr_v6: Uint8Array;
  control_permissions?: ControlPermissions | undefined;
  controlled_context?: ControlledContext | undefined;
}

export interface LocalAddr {
  socket_addr: Uint8Array;
  local_addr: Uint8Array;
  relay_server: string;
  id: string;
  version: string;
  socket_addr_v6: Uint8Array;
}

export interface PeerDiscovery {
  cmd: string;
  mac: string;
  id: string;
  username: string;
  hostname: string;
  platform: string;
  misc: string;
}

export interface OnlineRequest {
  id: string;
  peers: string[];
}

export interface OnlineResponse {
  states: Uint8Array;
}

export interface KeyExchange {
  keys: Uint8Array[];
}

export interface HealthCheck {
  token: string;
}

export interface HeaderEntry {
  name: string;
  value: string;
}

export interface HttpProxyRequest {
  method: string;
  path: string;
  headers: HeaderEntry[];
  body: Uint8Array;
}

export interface HttpProxyResponse {
  status: number;
  headers: HeaderEntry[];
  body: Uint8Array;
  error: string;
}

export interface RendezvousMessage {
  union?:
    | { $case: "register_peer"; register_peer: RegisterPeer }
    | { $case: "register_peer_response"; register_peer_response: RegisterPeerResponse }
    | { $case: "punch_hole_request"; punch_hole_request: PunchHoleRequest }
    | { $case: "punch_hole"; punch_hole: PunchHole }
    | { $case: "punch_hole_sent"; punch_hole_sent: PunchHoleSent }
    | { $case: "punch_hole_response"; punch_hole_response: PunchHoleResponse }
    | { $case: "fetch_local_addr"; fetch_local_addr: FetchLocalAddr }
    | { $case: "local_addr"; local_addr: LocalAddr }
    | { $case: "configure_update"; configure_update: ConfigUpdate }
    | { $case: "register_pk"; register_pk: RegisterPk }
    | { $case: "register_pk_response"; register_pk_response: RegisterPkResponse }
    | { $case: "software_update"; software_update: SoftwareUpdate }
    | { $case: "request_relay"; request_relay: RequestRelay }
    | { $case: "relay_response"; relay_response: RelayResponse }
    | { $case: "test_nat_request"; test_nat_request: TestNatRequest }
    | { $case: "test_nat_response"; test_nat_response: TestNatResponse }
    | { $case: "peer_discovery"; peer_discovery: PeerDiscovery }
    | { $case: "online_request"; online_request: OnlineRequest }
    | { $case: "online_response"; online_response: OnlineResponse }
    | { $case: "key_exchange"; key_exchange: KeyExchange }
    | { $case: "hc"; hc: HealthCheck }
    | { $case: "http_proxy_request"; http_proxy_request: HttpProxyRequest }
    | { $case: "http_proxy_response"; http_proxy_response: HttpProxyResponse }
    | undefined;
}

function createBaseRegisterPeer(): RegisterPeer {
  return { id: "", serial: 0 };
}

export const RegisterPeer: MessageFns<RegisterPeer> = {
  encode(message: RegisterPeer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.serial !== 0) {
      writer.uint32(16).int32(message.serial);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterPeer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterPeer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serial = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RegisterPeer>, I>>(base?: I): RegisterPeer {
    return RegisterPeer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterPeer>, I>>(object: I): RegisterPeer {
    const message = createBaseRegisterPeer();
    message.id = object.id ?? "";
    message.serial = object.serial ?? 0;
    return message;
  },
};

function createBaseRegisterPeerResponse(): RegisterPeerResponse {
  return { request_pk: false };
}

export const RegisterPeerResponse: MessageFns<RegisterPeerResponse> = {
  encode(message: RegisterPeerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.request_pk !== false) {
      writer.uint32(16).bool(message.request_pk);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterPeerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterPeerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.request_pk = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RegisterPeerResponse>, I>>(base?: I): RegisterPeerResponse {
    return RegisterPeerResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterPeerResponse>, I>>(object: I): RegisterPeerResponse {
    const message = createBaseRegisterPeerResponse();
    message.request_pk = object.request_pk ?? false;
    return message;
  },
};

function createBasePunchHoleRequest(): PunchHoleRequest {
  return {
    id: "",
    nat_type: 0,
    licence_key: "",
    conn_type: 0,
    token: "",
    version: "",
    udp_port: 0,
    force_relay: false,
    upnp_port: 0,
    socket_addr_v6: new Uint8Array(0),
    switch_code: "",
  };
}

export const PunchHoleRequest: MessageFns<PunchHoleRequest> = {
  encode(message: PunchHoleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.nat_type !== 0) {
      writer.uint32(16).int32(message.nat_type);
    }
    if (message.licence_key !== "") {
      writer.uint32(26).string(message.licence_key);
    }
    if (message.conn_type !== 0) {
      writer.uint32(32).int32(message.conn_type);
    }
    if (message.token !== "") {
      writer.uint32(42).string(message.token);
    }
    if (message.version !== "") {
      writer.uint32(50).string(message.version);
    }
    if (message.udp_port !== 0) {
      writer.uint32(56).int32(message.udp_port);
    }
    if (message.force_relay !== false) {
      writer.uint32(64).bool(message.force_relay);
    }
    if (message.upnp_port !== 0) {
      writer.uint32(72).int32(message.upnp_port);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(82).bytes(message.socket_addr_v6);
    }
    if (message.switch_code !== "") {
      writer.uint32(90).string(message.switch_code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PunchHoleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePunchHoleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nat_type = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.licence_key = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.conn_type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.udp_port = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.force_relay = reader.bool();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.upnp_port = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.switch_code = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PunchHoleRequest>, I>>(base?: I): PunchHoleRequest {
    return PunchHoleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PunchHoleRequest>, I>>(object: I): PunchHoleRequest {
    const message = createBasePunchHoleRequest();
    message.id = object.id ?? "";
    message.nat_type = object.nat_type ?? 0;
    message.licence_key = object.licence_key ?? "";
    message.conn_type = object.conn_type ?? 0;
    message.token = object.token ?? "";
    message.version = object.version ?? "";
    message.udp_port = object.udp_port ?? 0;
    message.force_relay = object.force_relay ?? false;
    message.upnp_port = object.upnp_port ?? 0;
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    message.switch_code = object.switch_code ?? "";
    return message;
  },
};

function createBaseControlPermissions(): ControlPermissions {
  return { permissions: 0n };
}

export const ControlPermissions: MessageFns<ControlPermissions> = {
  encode(message: ControlPermissions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.permissions !== 0n) {
      if (BigInt.asUintN(64, message.permissions) !== message.permissions) {
        throw new globalThis.Error("value provided for field message.permissions of type uint64 too large");
      }
      writer.uint32(8).uint64(message.permissions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlPermissions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlPermissions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.permissions = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ControlPermissions>, I>>(base?: I): ControlPermissions {
    return ControlPermissions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlPermissions>, I>>(object: I): ControlPermissions {
    const message = createBaseControlPermissions();
    message.permissions = (object.permissions !== undefined && object.permissions !== null)
      ? BigInt(object.permissions)
      : 0n;
    return message;
  },
};

function createBaseControlledContext(): ControlledContext {
  return { conn_audit_ref: "" };
}

export const ControlledContext: MessageFns<ControlledContext> = {
  encode(message: ControlledContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.conn_audit_ref !== "") {
      writer.uint32(10).string(message.conn_audit_ref);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ControlledContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControlledContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.conn_audit_ref = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ControlledContext>, I>>(base?: I): ControlledContext {
    return ControlledContext.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ControlledContext>, I>>(object: I): ControlledContext {
    const message = createBaseControlledContext();
    message.conn_audit_ref = object.conn_audit_ref ?? "";
    return message;
  },
};

function createBasePunchHole(): PunchHole {
  return {
    socket_addr: new Uint8Array(0),
    relay_server: "",
    nat_type: 0,
    udp_port: 0,
    force_relay: false,
    upnp_port: 0,
    socket_addr_v6: new Uint8Array(0),
    control_permissions: undefined,
    controlled_context: undefined,
  };
}

export const PunchHole: MessageFns<PunchHole> = {
  encode(message: PunchHole, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.socket_addr.length !== 0) {
      writer.uint32(10).bytes(message.socket_addr);
    }
    if (message.relay_server !== "") {
      writer.uint32(18).string(message.relay_server);
    }
    if (message.nat_type !== 0) {
      writer.uint32(24).int32(message.nat_type);
    }
    if (message.udp_port !== 0) {
      writer.uint32(32).int32(message.udp_port);
    }
    if (message.force_relay !== false) {
      writer.uint32(40).bool(message.force_relay);
    }
    if (message.upnp_port !== 0) {
      writer.uint32(48).int32(message.upnp_port);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(58).bytes(message.socket_addr_v6);
    }
    if (message.control_permissions !== undefined) {
      ControlPermissions.encode(message.control_permissions, writer.uint32(66).fork()).join();
    }
    if (message.controlled_context !== undefined) {
      ControlledContext.encode(message.controlled_context, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PunchHole {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePunchHole();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.nat_type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.udp_port = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.force_relay = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.upnp_port = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.control_permissions = ControlPermissions.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.controlled_context = ControlledContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PunchHole>, I>>(base?: I): PunchHole {
    return PunchHole.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PunchHole>, I>>(object: I): PunchHole {
    const message = createBasePunchHole();
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.relay_server = object.relay_server ?? "";
    message.nat_type = object.nat_type ?? 0;
    message.udp_port = object.udp_port ?? 0;
    message.force_relay = object.force_relay ?? false;
    message.upnp_port = object.upnp_port ?? 0;
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    message.control_permissions = (object.control_permissions !== undefined && object.control_permissions !== null)
      ? ControlPermissions.fromPartial(object.control_permissions)
      : undefined;
    message.controlled_context = (object.controlled_context !== undefined && object.controlled_context !== null)
      ? ControlledContext.fromPartial(object.controlled_context)
      : undefined;
    return message;
  },
};

function createBaseTestNatRequest(): TestNatRequest {
  return { serial: 0 };
}

export const TestNatRequest: MessageFns<TestNatRequest> = {
  encode(message: TestNatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serial !== 0) {
      writer.uint32(8).int32(message.serial);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestNatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestNatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.serial = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<TestNatRequest>, I>>(base?: I): TestNatRequest {
    return TestNatRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestNatRequest>, I>>(object: I): TestNatRequest {
    const message = createBaseTestNatRequest();
    message.serial = object.serial ?? 0;
    return message;
  },
};

function createBaseTestNatResponse(): TestNatResponse {
  return { port: 0, cu: undefined };
}

export const TestNatResponse: MessageFns<TestNatResponse> = {
  encode(message: TestNatResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.port !== 0) {
      writer.uint32(8).int32(message.port);
    }
    if (message.cu !== undefined) {
      ConfigUpdate.encode(message.cu, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestNatResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestNatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cu = ConfigUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<TestNatResponse>, I>>(base?: I): TestNatResponse {
    return TestNatResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TestNatResponse>, I>>(object: I): TestNatResponse {
    const message = createBaseTestNatResponse();
    message.port = object.port ?? 0;
    message.cu = (object.cu !== undefined && object.cu !== null) ? ConfigUpdate.fromPartial(object.cu) : undefined;
    return message;
  },
};

function createBasePunchHoleSent(): PunchHoleSent {
  return {
    socket_addr: new Uint8Array(0),
    id: "",
    relay_server: "",
    nat_type: 0,
    version: "",
    upnp_port: 0,
    socket_addr_v6: new Uint8Array(0),
  };
}

export const PunchHoleSent: MessageFns<PunchHoleSent> = {
  encode(message: PunchHoleSent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.socket_addr.length !== 0) {
      writer.uint32(10).bytes(message.socket_addr);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.relay_server !== "") {
      writer.uint32(26).string(message.relay_server);
    }
    if (message.nat_type !== 0) {
      writer.uint32(32).int32(message.nat_type);
    }
    if (message.version !== "") {
      writer.uint32(42).string(message.version);
    }
    if (message.upnp_port !== 0) {
      writer.uint32(48).int32(message.upnp_port);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(58).bytes(message.socket_addr_v6);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PunchHoleSent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePunchHoleSent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.nat_type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.upnp_port = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PunchHoleSent>, I>>(base?: I): PunchHoleSent {
    return PunchHoleSent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PunchHoleSent>, I>>(object: I): PunchHoleSent {
    const message = createBasePunchHoleSent();
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.id = object.id ?? "";
    message.relay_server = object.relay_server ?? "";
    message.nat_type = object.nat_type ?? 0;
    message.version = object.version ?? "";
    message.upnp_port = object.upnp_port ?? 0;
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRegisterPk(): RegisterPk {
  return { id: "", uuid: new Uint8Array(0), pk: new Uint8Array(0), old_id: "", no_register_device: false };
}

export const RegisterPk: MessageFns<RegisterPk> = {
  encode(message: RegisterPk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.uuid.length !== 0) {
      writer.uint32(18).bytes(message.uuid);
    }
    if (message.pk.length !== 0) {
      writer.uint32(26).bytes(message.pk);
    }
    if (message.old_id !== "") {
      writer.uint32(34).string(message.old_id);
    }
    if (message.no_register_device !== false) {
      writer.uint32(40).bool(message.no_register_device);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterPk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterPk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uuid = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pk = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.old_id = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.no_register_device = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RegisterPk>, I>>(base?: I): RegisterPk {
    return RegisterPk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterPk>, I>>(object: I): RegisterPk {
    const message = createBaseRegisterPk();
    message.id = object.id ?? "";
    message.uuid = object.uuid ?? new Uint8Array(0);
    message.pk = object.pk ?? new Uint8Array(0);
    message.old_id = object.old_id ?? "";
    message.no_register_device = object.no_register_device ?? false;
    return message;
  },
};

function createBaseRegisterPkResponse(): RegisterPkResponse {
  return { result: 0, keep_alive: 0 };
}

export const RegisterPkResponse: MessageFns<RegisterPkResponse> = {
  encode(message: RegisterPkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.result !== 0) {
      writer.uint32(8).int32(message.result);
    }
    if (message.keep_alive !== 0) {
      writer.uint32(16).int32(message.keep_alive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterPkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterPkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.result = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.keep_alive = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RegisterPkResponse>, I>>(base?: I): RegisterPkResponse {
    return RegisterPkResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterPkResponse>, I>>(object: I): RegisterPkResponse {
    const message = createBaseRegisterPkResponse();
    message.result = object.result ?? 0;
    message.keep_alive = object.keep_alive ?? 0;
    return message;
  },
};

function createBasePunchHoleResponse(): PunchHoleResponse {
  return {
    socket_addr: new Uint8Array(0),
    pk: new Uint8Array(0),
    failure: 0,
    relay_server: "",
    union: undefined,
    other_failure: "",
    feedback: 0,
    is_udp: false,
    upnp_port: 0,
    socket_addr_v6: new Uint8Array(0),
  };
}

export const PunchHoleResponse: MessageFns<PunchHoleResponse> = {
  encode(message: PunchHoleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.socket_addr.length !== 0) {
      writer.uint32(10).bytes(message.socket_addr);
    }
    if (message.pk.length !== 0) {
      writer.uint32(18).bytes(message.pk);
    }
    if (message.failure !== 0) {
      writer.uint32(24).int32(message.failure);
    }
    if (message.relay_server !== "") {
      writer.uint32(34).string(message.relay_server);
    }
    switch (message.union?.$case) {
      case "nat_type":
        writer.uint32(40).int32(message.union.nat_type);
        break;
      case "is_local":
        writer.uint32(48).bool(message.union.is_local);
        break;
    }
    if (message.other_failure !== "") {
      writer.uint32(58).string(message.other_failure);
    }
    if (message.feedback !== 0) {
      writer.uint32(64).int32(message.feedback);
    }
    if (message.is_udp !== false) {
      writer.uint32(72).bool(message.is_udp);
    }
    if (message.upnp_port !== 0) {
      writer.uint32(80).int32(message.upnp_port);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(90).bytes(message.socket_addr_v6);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PunchHoleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePunchHoleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pk = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.failure = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.union = { $case: "nat_type", nat_type: reader.int32() as any };
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.union = { $case: "is_local", is_local: reader.bool() };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.other_failure = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.feedback = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.is_udp = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.upnp_port = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PunchHoleResponse>, I>>(base?: I): PunchHoleResponse {
    return PunchHoleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PunchHoleResponse>, I>>(object: I): PunchHoleResponse {
    const message = createBasePunchHoleResponse();
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.pk = object.pk ?? new Uint8Array(0);
    message.failure = object.failure ?? 0;
    message.relay_server = object.relay_server ?? "";
    switch (object.union?.$case) {
      case "nat_type": {
        if (object.union?.nat_type !== undefined && object.union?.nat_type !== null) {
          message.union = { $case: "nat_type", nat_type: object.union.nat_type };
        }
        break;
      }
      case "is_local": {
        if (object.union?.is_local !== undefined && object.union?.is_local !== null) {
          message.union = { $case: "is_local", is_local: object.union.is_local };
        }
        break;
      }
    }
    message.other_failure = object.other_failure ?? "";
    message.feedback = object.feedback ?? 0;
    message.is_udp = object.is_udp ?? false;
    message.upnp_port = object.upnp_port ?? 0;
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    return message;
  },
};

function createBaseConfigUpdate(): ConfigUpdate {
  return { serial: 0, rendezvous_servers: [] };
}

export const ConfigUpdate: MessageFns<ConfigUpdate> = {
  encode(message: ConfigUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serial !== 0) {
      writer.uint32(8).int32(message.serial);
    }
    for (const v of message.rendezvous_servers) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.serial = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rendezvous_servers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ConfigUpdate>, I>>(base?: I): ConfigUpdate {
    return ConfigUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigUpdate>, I>>(object: I): ConfigUpdate {
    const message = createBaseConfigUpdate();
    message.serial = object.serial ?? 0;
    message.rendezvous_servers = object.rendezvous_servers?.map((e) => e) || [];
    return message;
  },
};

function createBaseRequestRelay(): RequestRelay {
  return {
    id: "",
    uuid: "",
    socket_addr: new Uint8Array(0),
    relay_server: "",
    secure: false,
    licence_key: "",
    conn_type: 0,
    token: "",
    control_permissions: undefined,
    controlled_context: undefined,
    switch_code: "",
  };
}

export const RequestRelay: MessageFns<RequestRelay> = {
  encode(message: RequestRelay, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.uuid !== "") {
      writer.uint32(18).string(message.uuid);
    }
    if (message.socket_addr.length !== 0) {
      writer.uint32(26).bytes(message.socket_addr);
    }
    if (message.relay_server !== "") {
      writer.uint32(34).string(message.relay_server);
    }
    if (message.secure !== false) {
      writer.uint32(40).bool(message.secure);
    }
    if (message.licence_key !== "") {
      writer.uint32(50).string(message.licence_key);
    }
    if (message.conn_type !== 0) {
      writer.uint32(56).int32(message.conn_type);
    }
    if (message.token !== "") {
      writer.uint32(66).string(message.token);
    }
    if (message.control_permissions !== undefined) {
      ControlPermissions.encode(message.control_permissions, writer.uint32(74).fork()).join();
    }
    if (message.controlled_context !== undefined) {
      ControlledContext.encode(message.controlled_context, writer.uint32(82).fork()).join();
    }
    if (message.switch_code !== "") {
      writer.uint32(90).string(message.switch_code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestRelay {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestRelay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.secure = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.licence_key = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.conn_type = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.control_permissions = ControlPermissions.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.controlled_context = ControlledContext.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.switch_code = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RequestRelay>, I>>(base?: I): RequestRelay {
    return RequestRelay.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestRelay>, I>>(object: I): RequestRelay {
    const message = createBaseRequestRelay();
    message.id = object.id ?? "";
    message.uuid = object.uuid ?? "";
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.relay_server = object.relay_server ?? "";
    message.secure = object.secure ?? false;
    message.licence_key = object.licence_key ?? "";
    message.conn_type = object.conn_type ?? 0;
    message.token = object.token ?? "";
    message.control_permissions = (object.control_permissions !== undefined && object.control_permissions !== null)
      ? ControlPermissions.fromPartial(object.control_permissions)
      : undefined;
    message.controlled_context = (object.controlled_context !== undefined && object.controlled_context !== null)
      ? ControlledContext.fromPartial(object.controlled_context)
      : undefined;
    message.switch_code = object.switch_code ?? "";
    return message;
  },
};

function createBaseRelayResponse(): RelayResponse {
  return {
    socket_addr: new Uint8Array(0),
    uuid: "",
    relay_server: "",
    union: undefined,
    refuse_reason: "",
    version: "",
    feedback: 0,
    socket_addr_v6: new Uint8Array(0),
    upnp_port: 0,
  };
}

export const RelayResponse: MessageFns<RelayResponse> = {
  encode(message: RelayResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.socket_addr.length !== 0) {
      writer.uint32(10).bytes(message.socket_addr);
    }
    if (message.uuid !== "") {
      writer.uint32(18).string(message.uuid);
    }
    if (message.relay_server !== "") {
      writer.uint32(26).string(message.relay_server);
    }
    switch (message.union?.$case) {
      case "id":
        writer.uint32(34).string(message.union.id);
        break;
      case "pk":
        writer.uint32(42).bytes(message.union.pk);
        break;
    }
    if (message.refuse_reason !== "") {
      writer.uint32(50).string(message.refuse_reason);
    }
    if (message.version !== "") {
      writer.uint32(58).string(message.version);
    }
    if (message.feedback !== 0) {
      writer.uint32(72).int32(message.feedback);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(82).bytes(message.socket_addr_v6);
    }
    if (message.upnp_port !== 0) {
      writer.uint32(88).int32(message.upnp_port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RelayResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRelayResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.union = { $case: "id", id: reader.string() };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.union = { $case: "pk", pk: reader.bytes() };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.refuse_reason = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.feedback = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.upnp_port = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RelayResponse>, I>>(base?: I): RelayResponse {
    return RelayResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RelayResponse>, I>>(object: I): RelayResponse {
    const message = createBaseRelayResponse();
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.uuid = object.uuid ?? "";
    message.relay_server = object.relay_server ?? "";
    switch (object.union?.$case) {
      case "id": {
        if (object.union?.id !== undefined && object.union?.id !== null) {
          message.union = { $case: "id", id: object.union.id };
        }
        break;
      }
      case "pk": {
        if (object.union?.pk !== undefined && object.union?.pk !== null) {
          message.union = { $case: "pk", pk: object.union.pk };
        }
        break;
      }
    }
    message.refuse_reason = object.refuse_reason ?? "";
    message.version = object.version ?? "";
    message.feedback = object.feedback ?? 0;
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    message.upnp_port = object.upnp_port ?? 0;
    return message;
  },
};

function createBaseSoftwareUpdate(): SoftwareUpdate {
  return { url: "" };
}

export const SoftwareUpdate: MessageFns<SoftwareUpdate> = {
  encode(message: SoftwareUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SoftwareUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSoftwareUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<SoftwareUpdate>, I>>(base?: I): SoftwareUpdate {
    return SoftwareUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SoftwareUpdate>, I>>(object: I): SoftwareUpdate {
    const message = createBaseSoftwareUpdate();
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseFetchLocalAddr(): FetchLocalAddr {
  return {
    socket_addr: new Uint8Array(0),
    relay_server: "",
    socket_addr_v6: new Uint8Array(0),
    control_permissions: undefined,
    controlled_context: undefined,
  };
}

export const FetchLocalAddr: MessageFns<FetchLocalAddr> = {
  encode(message: FetchLocalAddr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.socket_addr.length !== 0) {
      writer.uint32(10).bytes(message.socket_addr);
    }
    if (message.relay_server !== "") {
      writer.uint32(18).string(message.relay_server);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(26).bytes(message.socket_addr_v6);
    }
    if (message.control_permissions !== undefined) {
      ControlPermissions.encode(message.control_permissions, writer.uint32(34).fork()).join();
    }
    if (message.controlled_context !== undefined) {
      ControlledContext.encode(message.controlled_context, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FetchLocalAddr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFetchLocalAddr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.control_permissions = ControlPermissions.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.controlled_context = ControlledContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<FetchLocalAddr>, I>>(base?: I): FetchLocalAddr {
    return FetchLocalAddr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FetchLocalAddr>, I>>(object: I): FetchLocalAddr {
    const message = createBaseFetchLocalAddr();
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.relay_server = object.relay_server ?? "";
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    message.control_permissions = (object.control_permissions !== undefined && object.control_permissions !== null)
      ? ControlPermissions.fromPartial(object.control_permissions)
      : undefined;
    message.controlled_context = (object.controlled_context !== undefined && object.controlled_context !== null)
      ? ControlledContext.fromPartial(object.controlled_context)
      : undefined;
    return message;
  },
};

function createBaseLocalAddr(): LocalAddr {
  return {
    socket_addr: new Uint8Array(0),
    local_addr: new Uint8Array(0),
    relay_server: "",
    id: "",
    version: "",
    socket_addr_v6: new Uint8Array(0),
  };
}

export const LocalAddr: MessageFns<LocalAddr> = {
  encode(message: LocalAddr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.socket_addr.length !== 0) {
      writer.uint32(10).bytes(message.socket_addr);
    }
    if (message.local_addr.length !== 0) {
      writer.uint32(18).bytes(message.local_addr);
    }
    if (message.relay_server !== "") {
      writer.uint32(26).string(message.relay_server);
    }
    if (message.id !== "") {
      writer.uint32(34).string(message.id);
    }
    if (message.version !== "") {
      writer.uint32(42).string(message.version);
    }
    if (message.socket_addr_v6.length !== 0) {
      writer.uint32(50).bytes(message.socket_addr_v6);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LocalAddr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocalAddr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.socket_addr = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.local_addr = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.relay_server = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.socket_addr_v6 = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<LocalAddr>, I>>(base?: I): LocalAddr {
    return LocalAddr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LocalAddr>, I>>(object: I): LocalAddr {
    const message = createBaseLocalAddr();
    message.socket_addr = object.socket_addr ?? new Uint8Array(0);
    message.local_addr = object.local_addr ?? new Uint8Array(0);
    message.relay_server = object.relay_server ?? "";
    message.id = object.id ?? "";
    message.version = object.version ?? "";
    message.socket_addr_v6 = object.socket_addr_v6 ?? new Uint8Array(0);
    return message;
  },
};

function createBasePeerDiscovery(): PeerDiscovery {
  return { cmd: "", mac: "", id: "", username: "", hostname: "", platform: "", misc: "" };
}

export const PeerDiscovery: MessageFns<PeerDiscovery> = {
  encode(message: PeerDiscovery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cmd !== "") {
      writer.uint32(10).string(message.cmd);
    }
    if (message.mac !== "") {
      writer.uint32(18).string(message.mac);
    }
    if (message.id !== "") {
      writer.uint32(26).string(message.id);
    }
    if (message.username !== "") {
      writer.uint32(34).string(message.username);
    }
    if (message.hostname !== "") {
      writer.uint32(42).string(message.hostname);
    }
    if (message.platform !== "") {
      writer.uint32(50).string(message.platform);
    }
    if (message.misc !== "") {
      writer.uint32(58).string(message.misc);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PeerDiscovery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePeerDiscovery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cmd = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mac = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.misc = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PeerDiscovery>, I>>(base?: I): PeerDiscovery {
    return PeerDiscovery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PeerDiscovery>, I>>(object: I): PeerDiscovery {
    const message = createBasePeerDiscovery();
    message.cmd = object.cmd ?? "";
    message.mac = object.mac ?? "";
    message.id = object.id ?? "";
    message.username = object.username ?? "";
    message.hostname = object.hostname ?? "";
    message.platform = object.platform ?? "";
    message.misc = object.misc ?? "";
    return message;
  },
};

function createBaseOnlineRequest(): OnlineRequest {
  return { id: "", peers: [] };
}

export const OnlineRequest: MessageFns<OnlineRequest> = {
  encode(message: OnlineRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.peers) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OnlineRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOnlineRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.peers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<OnlineRequest>, I>>(base?: I): OnlineRequest {
    return OnlineRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OnlineRequest>, I>>(object: I): OnlineRequest {
    const message = createBaseOnlineRequest();
    message.id = object.id ?? "";
    message.peers = object.peers?.map((e) => e) || [];
    return message;
  },
};

function createBaseOnlineResponse(): OnlineResponse {
  return { states: new Uint8Array(0) };
}

export const OnlineResponse: MessageFns<OnlineResponse> = {
  encode(message: OnlineResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.states.length !== 0) {
      writer.uint32(10).bytes(message.states);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OnlineResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOnlineResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.states = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<OnlineResponse>, I>>(base?: I): OnlineResponse {
    return OnlineResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OnlineResponse>, I>>(object: I): OnlineResponse {
    const message = createBaseOnlineResponse();
    message.states = object.states ?? new Uint8Array(0);
    return message;
  },
};

function createBaseKeyExchange(): KeyExchange {
  return { keys: [] };
}

export const KeyExchange: MessageFns<KeyExchange> = {
  encode(message: KeyExchange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keys) {
      writer.uint32(10).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KeyExchange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKeyExchange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keys.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<KeyExchange>, I>>(base?: I): KeyExchange {
    return KeyExchange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KeyExchange>, I>>(object: I): KeyExchange {
    const message = createBaseKeyExchange();
    message.keys = object.keys?.map((e) => e) || [];
    return message;
  },
};

function createBaseHealthCheck(): HealthCheck {
  return { token: "" };
}

export const HealthCheck: MessageFns<HealthCheck> = {
  encode(message: HealthCheck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthCheck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthCheck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HealthCheck>, I>>(base?: I): HealthCheck {
    return HealthCheck.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthCheck>, I>>(object: I): HealthCheck {
    const message = createBaseHealthCheck();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseHeaderEntry(): HeaderEntry {
  return { name: "", value: "" };
}

export const HeaderEntry: MessageFns<HeaderEntry> = {
  encode(message: HeaderEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeaderEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeaderEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HeaderEntry>, I>>(base?: I): HeaderEntry {
    return HeaderEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeaderEntry>, I>>(object: I): HeaderEntry {
    const message = createBaseHeaderEntry();
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseHttpProxyRequest(): HttpProxyRequest {
  return { method: "", path: "", headers: [], body: new Uint8Array(0) };
}

export const HttpProxyRequest: MessageFns<HttpProxyRequest> = {
  encode(message: HttpProxyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.method !== "") {
      writer.uint32(10).string(message.method);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    for (const v of message.headers) {
      HeaderEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.body.length !== 0) {
      writer.uint32(34).bytes(message.body);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpProxyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpProxyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.method = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.headers.push(HeaderEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HttpProxyRequest>, I>>(base?: I): HttpProxyRequest {
    return HttpProxyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpProxyRequest>, I>>(object: I): HttpProxyRequest {
    const message = createBaseHttpProxyRequest();
    message.method = object.method ?? "";
    message.path = object.path ?? "";
    message.headers = object.headers?.map((e) => HeaderEntry.fromPartial(e)) || [];
    message.body = object.body ?? new Uint8Array(0);
    return message;
  },
};

function createBaseHttpProxyResponse(): HttpProxyResponse {
  return { status: 0, headers: [], body: new Uint8Array(0), error: "" };
}

export const HttpProxyResponse: MessageFns<HttpProxyResponse> = {
  encode(message: HttpProxyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    for (const v of message.headers) {
      HeaderEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.body.length !== 0) {
      writer.uint32(26).bytes(message.body);
    }
    if (message.error !== "") {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HttpProxyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHttpProxyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.headers.push(HeaderEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.body = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<HttpProxyResponse>, I>>(base?: I): HttpProxyResponse {
    return HttpProxyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HttpProxyResponse>, I>>(object: I): HttpProxyResponse {
    const message = createBaseHttpProxyResponse();
    message.status = object.status ?? 0;
    message.headers = object.headers?.map((e) => HeaderEntry.fromPartial(e)) || [];
    message.body = object.body ?? new Uint8Array(0);
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseRendezvousMessage(): RendezvousMessage {
  return { union: undefined };
}

export const RendezvousMessage: MessageFns<RendezvousMessage> = {
  encode(message: RendezvousMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.union?.$case) {
      case "register_peer":
        RegisterPeer.encode(message.union.register_peer, writer.uint32(50).fork()).join();
        break;
      case "register_peer_response":
        RegisterPeerResponse.encode(message.union.register_peer_response, writer.uint32(58).fork()).join();
        break;
      case "punch_hole_request":
        PunchHoleRequest.encode(message.union.punch_hole_request, writer.uint32(66).fork()).join();
        break;
      case "punch_hole":
        PunchHole.encode(message.union.punch_hole, writer.uint32(74).fork()).join();
        break;
      case "punch_hole_sent":
        PunchHoleSent.encode(message.union.punch_hole_sent, writer.uint32(82).fork()).join();
        break;
      case "punch_hole_response":
        PunchHoleResponse.encode(message.union.punch_hole_response, writer.uint32(90).fork()).join();
        break;
      case "fetch_local_addr":
        FetchLocalAddr.encode(message.union.fetch_local_addr, writer.uint32(98).fork()).join();
        break;
      case "local_addr":
        LocalAddr.encode(message.union.local_addr, writer.uint32(106).fork()).join();
        break;
      case "configure_update":
        ConfigUpdate.encode(message.union.configure_update, writer.uint32(114).fork()).join();
        break;
      case "register_pk":
        RegisterPk.encode(message.union.register_pk, writer.uint32(122).fork()).join();
        break;
      case "register_pk_response":
        RegisterPkResponse.encode(message.union.register_pk_response, writer.uint32(130).fork()).join();
        break;
      case "software_update":
        SoftwareUpdate.encode(message.union.software_update, writer.uint32(138).fork()).join();
        break;
      case "request_relay":
        RequestRelay.encode(message.union.request_relay, writer.uint32(146).fork()).join();
        break;
      case "relay_response":
        RelayResponse.encode(message.union.relay_response, writer.uint32(154).fork()).join();
        break;
      case "test_nat_request":
        TestNatRequest.encode(message.union.test_nat_request, writer.uint32(162).fork()).join();
        break;
      case "test_nat_response":
        TestNatResponse.encode(message.union.test_nat_response, writer.uint32(170).fork()).join();
        break;
      case "peer_discovery":
        PeerDiscovery.encode(message.union.peer_discovery, writer.uint32(178).fork()).join();
        break;
      case "online_request":
        OnlineRequest.encode(message.union.online_request, writer.uint32(186).fork()).join();
        break;
      case "online_response":
        OnlineResponse.encode(message.union.online_response, writer.uint32(194).fork()).join();
        break;
      case "key_exchange":
        KeyExchange.encode(message.union.key_exchange, writer.uint32(202).fork()).join();
        break;
      case "hc":
        HealthCheck.encode(message.union.hc, writer.uint32(210).fork()).join();
        break;
      case "http_proxy_request":
        HttpProxyRequest.encode(message.union.http_proxy_request, writer.uint32(218).fork()).join();
        break;
      case "http_proxy_response":
        HttpProxyResponse.encode(message.union.http_proxy_response, writer.uint32(226).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RendezvousMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRendezvousMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.union = { $case: "register_peer", register_peer: RegisterPeer.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.union = {
            $case: "register_peer_response",
            register_peer_response: RegisterPeerResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.union = {
            $case: "punch_hole_request",
            punch_hole_request: PunchHoleRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.union = { $case: "punch_hole", punch_hole: PunchHole.decode(reader, reader.uint32()) };
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.union = { $case: "punch_hole_sent", punch_hole_sent: PunchHoleSent.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.union = {
            $case: "punch_hole_response",
            punch_hole_response: PunchHoleResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.union = {
            $case: "fetch_local_addr",
            fetch_local_addr: FetchLocalAddr.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.union = { $case: "local_addr", local_addr: LocalAddr.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.union = { $case: "configure_update", configure_update: ConfigUpdate.decode(reader, reader.uint32()) };
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.union = { $case: "register_pk", register_pk: RegisterPk.decode(reader, reader.uint32()) };
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.union = {
            $case: "register_pk_response",
            register_pk_response: RegisterPkResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.union = { $case: "software_update", software_update: SoftwareUpdate.decode(reader, reader.uint32()) };
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.union = { $case: "request_relay", request_relay: RequestRelay.decode(reader, reader.uint32()) };
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.union = { $case: "relay_response", relay_response: RelayResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.union = {
            $case: "test_nat_request",
            test_nat_request: TestNatRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.union = {
            $case: "test_nat_response",
            test_nat_response: TestNatResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          message.union = { $case: "peer_discovery", peer_discovery: PeerDiscovery.decode(reader, reader.uint32()) };
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          message.union = { $case: "online_request", online_request: OnlineRequest.decode(reader, reader.uint32()) };
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.union = { $case: "online_response", online_response: OnlineResponse.decode(reader, reader.uint32()) };
          continue;
        }
        case 25: {
          if (tag !== 202) {
            break;
          }

          message.union = { $case: "key_exchange", key_exchange: KeyExchange.decode(reader, reader.uint32()) };
          continue;
        }
        case 26: {
          if (tag !== 210) {
            break;
          }

          message.union = { $case: "hc", hc: HealthCheck.decode(reader, reader.uint32()) };
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.union = {
            $case: "http_proxy_request",
            http_proxy_request: HttpProxyRequest.decode(reader, reader.uint32()),
          };
          continue;
        }
        case 28: {
          if (tag !== 226) {
            break;
          }

          message.union = {
            $case: "http_proxy_response",
            http_proxy_response: HttpProxyResponse.decode(reader, reader.uint32()),
          };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RendezvousMessage>, I>>(base?: I): RendezvousMessage {
    return RendezvousMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RendezvousMessage>, I>>(object: I): RendezvousMessage {
    const message = createBaseRendezvousMessage();
    switch (object.union?.$case) {
      case "register_peer": {
        if (object.union?.register_peer !== undefined && object.union?.register_peer !== null) {
          message.union = {
            $case: "register_peer",
            register_peer: RegisterPeer.fromPartial(object.union.register_peer),
          };
        }
        break;
      }
      case "register_peer_response": {
        if (object.union?.register_peer_response !== undefined && object.union?.register_peer_response !== null) {
          message.union = {
            $case: "register_peer_response",
            register_peer_response: RegisterPeerResponse.fromPartial(object.union.register_peer_response),
          };
        }
        break;
      }
      case "punch_hole_request": {
        if (object.union?.punch_hole_request !== undefined && object.union?.punch_hole_request !== null) {
          message.union = {
            $case: "punch_hole_request",
            punch_hole_request: PunchHoleRequest.fromPartial(object.union.punch_hole_request),
          };
        }
        break;
      }
      case "punch_hole": {
        if (object.union?.punch_hole !== undefined && object.union?.punch_hole !== null) {
          message.union = { $case: "punch_hole", punch_hole: PunchHole.fromPartial(object.union.punch_hole) };
        }
        break;
      }
      case "punch_hole_sent": {
        if (object.union?.punch_hole_sent !== undefined && object.union?.punch_hole_sent !== null) {
          message.union = {
            $case: "punch_hole_sent",
            punch_hole_sent: PunchHoleSent.fromPartial(object.union.punch_hole_sent),
          };
        }
        break;
      }
      case "punch_hole_response": {
        if (object.union?.punch_hole_response !== undefined && object.union?.punch_hole_response !== null) {
          message.union = {
            $case: "punch_hole_response",
            punch_hole_response: PunchHoleResponse.fromPartial(object.union.punch_hole_response),
          };
        }
        break;
      }
      case "fetch_local_addr": {
        if (object.union?.fetch_local_addr !== undefined && object.union?.fetch_local_addr !== null) {
          message.union = {
            $case: "fetch_local_addr",
            fetch_local_addr: FetchLocalAddr.fromPartial(object.union.fetch_local_addr),
          };
        }
        break;
      }
      case "local_addr": {
        if (object.union?.local_addr !== undefined && object.union?.local_addr !== null) {
          message.union = { $case: "local_addr", local_addr: LocalAddr.fromPartial(object.union.local_addr) };
        }
        break;
      }
      case "configure_update": {
        if (object.union?.configure_update !== undefined && object.union?.configure_update !== null) {
          message.union = {
            $case: "configure_update",
            configure_update: ConfigUpdate.fromPartial(object.union.configure_update),
          };
        }
        break;
      }
      case "register_pk": {
        if (object.union?.register_pk !== undefined && object.union?.register_pk !== null) {
          message.union = { $case: "register_pk", register_pk: RegisterPk.fromPartial(object.union.register_pk) };
        }
        break;
      }
      case "register_pk_response": {
        if (object.union?.register_pk_response !== undefined && object.union?.register_pk_response !== null) {
          message.union = {
            $case: "register_pk_response",
            register_pk_response: RegisterPkResponse.fromPartial(object.union.register_pk_response),
          };
        }
        break;
      }
      case "software_update": {
        if (object.union?.software_update !== undefined && object.union?.software_update !== null) {
          message.union = {
            $case: "software_update",
            software_update: SoftwareUpdate.fromPartial(object.union.software_update),
          };
        }
        break;
      }
      case "request_relay": {
        if (object.union?.request_relay !== undefined && object.union?.request_relay !== null) {
          message.union = {
            $case: "request_relay",
            request_relay: RequestRelay.fromPartial(object.union.request_relay),
          };
        }
        break;
      }
      case "relay_response": {
        if (object.union?.relay_response !== undefined && object.union?.relay_response !== null) {
          message.union = {
            $case: "relay_response",
            relay_response: RelayResponse.fromPartial(object.union.relay_response),
          };
        }
        break;
      }
      case "test_nat_request": {
        if (object.union?.test_nat_request !== undefined && object.union?.test_nat_request !== null) {
          message.union = {
            $case: "test_nat_request",
            test_nat_request: TestNatRequest.fromPartial(object.union.test_nat_request),
          };
        }
        break;
      }
      case "test_nat_response": {
        if (object.union?.test_nat_response !== undefined && object.union?.test_nat_response !== null) {
          message.union = {
            $case: "test_nat_response",
            test_nat_response: TestNatResponse.fromPartial(object.union.test_nat_response),
          };
        }
        break;
      }
      case "peer_discovery": {
        if (object.union?.peer_discovery !== undefined && object.union?.peer_discovery !== null) {
          message.union = {
            $case: "peer_discovery",
            peer_discovery: PeerDiscovery.fromPartial(object.union.peer_discovery),
          };
        }
        break;
      }
      case "online_request": {
        if (object.union?.online_request !== undefined && object.union?.online_request !== null) {
          message.union = {
            $case: "online_request",
            online_request: OnlineRequest.fromPartial(object.union.online_request),
          };
        }
        break;
      }
      case "online_response": {
        if (object.union?.online_response !== undefined && object.union?.online_response !== null) {
          message.union = {
            $case: "online_response",
            online_response: OnlineResponse.fromPartial(object.union.online_response),
          };
        }
        break;
      }
      case "key_exchange": {
        if (object.union?.key_exchange !== undefined && object.union?.key_exchange !== null) {
          message.union = { $case: "key_exchange", key_exchange: KeyExchange.fromPartial(object.union.key_exchange) };
        }
        break;
      }
      case "hc": {
        if (object.union?.hc !== undefined && object.union?.hc !== null) {
          message.union = { $case: "hc", hc: HealthCheck.fromPartial(object.union.hc) };
        }
        break;
      }
      case "http_proxy_request": {
        if (object.union?.http_proxy_request !== undefined && object.union?.http_proxy_request !== null) {
          message.union = {
            $case: "http_proxy_request",
            http_proxy_request: HttpProxyRequest.fromPartial(object.union.http_proxy_request),
          };
        }
        break;
      }
      case "http_proxy_response": {
        if (object.union?.http_proxy_response !== undefined && object.union?.http_proxy_response !== null) {
          message.union = {
            $case: "http_proxy_response",
            http_proxy_response: HttpProxyResponse.fromPartial(object.union.http_proxy_response),
          };
        }
        break;
      }
    }
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
